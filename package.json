{
  "name": "config-one",
  "version": "0.0.1",
  "description": "One config library to rule them all",
  "main": "dist/main.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "//": "FIXME: need to change this back to `vows 'test/**/test*.js' --spec`",
    "test": "vows 'test/**/test-*.js' --spec",
    "build": "babel src --presets babel-preset-es2015 --out-dir dist",
    "prepublish": "npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Klortho/config-one.git"
  },
  "keywords": [
    "config",
    "conf",
    "configuration",
    "env",
    "environment"
  ],
  "author": "Chris Maloney",
  "license": "WTFPL",
  "bugs": {
    "url": "https://github.com/Klortho/config-one/issues"
  },
  "homepage": "https://github.com/Klortho/config-one#readme",
  "devDependencies": {
    "babel-cli": "^6.8.0",
    "babel-preset-es2015": "^6.6.0",
    "vows": "^0.8.1",
    "webpack": "^1.13.1"
  },
  "dependencies": {
    "extend": "^3.0.0",
    "ramda": "^0.21.0"
  }
}
