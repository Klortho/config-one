# Tests

This test suite uses mocha. Any file here or in a subdirectory that matches
`test-*.js` is a test.

So, you could run them with, for example

```
find ./test -name 'test-*' | xargs mocha
```
